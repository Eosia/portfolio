<nav class="navbar navbar-expand-lg navbar-dark" id="nav">
    <a class="navbar-brand" href="{{ path('accueil') }}">

        <!--logo cliquable-->
        <figure class="img-fluid">
            <img src="{{ asset('assets/img/favicon/logo_noir.png') }}" loading="lazy"
                 title="logo eosia"
                 alt="logo eosia" style="width: 96px">
            <!--figcaption-->
            <figcaption class="hidden">
                logo eosia
            </figcaption>
        </figure>
    </a>


    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto container">
            <li class="nav-item active">
                <a class="nav-link" href={{ path('accueil') }}>ACCUEIL<span class="sr-only">(current)</span></a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('portfolio') }}">PORTFOLIO</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('skills') }}">SKILLS</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('contact') }}">CONTACT</a>
            </li>

            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link link-admin" href="{{ path('admin') }}">ADMIN</a>
                </li>

            {% else %}

                <li class="nav-item">
                    <a class="nav-link link-disable disabled" href="#" tabindex="-1" aria-disabled="true">ADMIN</a>
                </li>

            {% endif %}

        </ul>

    </div>
</nav>